//gig

const Gig = (function() {
  return class Gig {
    constructor({ id, title, body, poster, tag }) {
      this.id = id;
      this.title = title;
      this.body = body;
      this.poster_id = poster.id;
      this.tag_id = tag.id;
    }

    static renderGig(){
      
    }
  };
})();
